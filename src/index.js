import React from 'react';
import ReactDOM from 'react-dom';
import TrilemmaSwitches from './TrilemmaSwitches';

ReactDOM.render(
  <React.StrictMode>
    <TrilemmaSwitches />
  </React.StrictMode>,
  document.getElementById('root')
);
